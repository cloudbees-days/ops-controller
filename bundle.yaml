apiVersion: "1"
version: "1"
id: "cloudbees-ci-casc-REPLACE_CONTROLLER_NAME"
description: "CloudBees CI configuration bundle for the CloudBees CI CasC Workshop REPLACE_CONTROLLER_NAME Controller"
jcasc:
  - "jenkins.yaml"
plugins:
  - "plugins.yaml"
